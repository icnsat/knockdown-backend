from django.urls import path, include
from rest_framework.routers import DefaultRouter
from . import views

# router = DefaultRouter()
# router.register('', views.UserViewSet)  # intended for admins, but we'll leave administration in the Django admin panel

# urlpatterns = [

# ] + router.urls
